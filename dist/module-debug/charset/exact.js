import { EncoderOptions } from "../type.js";

/** @type EncoderOptions */
const options = {
    normalize: !1,
    numeric: !1,
    dedupe: !1
    //split: /\s+/
    //normalize: false,
    //dedupe: false
};
export default options;