import { EncoderOptions } from "../type.js";

/** @type EncoderOptions */
const options = {
    normalize: false,
    numeric: false,
    dedupe: false
    //split: /\s+/
    //normalize: false,
    //dedupe: false
};
export default options;